server: 
  port: 8383 
spring: 
  application: 
    name: gateway-service 
  cloud: 
    config: 
      discovery: 
        serviceId: config-server
        enabled: true
  config: 
   import: "optional:configserver:"  
cloud:
 gateway:
  routes:
   - id: checkout-service
     uri: lb://checkout-service
     predicates:
       - Path=/api/checkout
